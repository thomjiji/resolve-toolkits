usage: proxy.py [-h] input output

Proxy is a commandline tool to automatic import clips, create timelines, add to render 
queue using the predefined preset.

positional arguments:
  input       Input path of source media.
  output      Output path of proxy rendering.

options:
  -h, --help  show this help message and exit